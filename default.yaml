schema_list:
  - schema: moe_pinyin

# Rime default settings
# encoding: utf-8

config_version: '0.40'

menu:
  page_size: 5

ascii_composer:
  good_old_caps_lock: true
  switch_key:
    Shift_L: inline_ascii
    Shift_R: commit_text
    Control_L: noop
    Control_R: noop
    Caps_Lock: clear
    Eisu_toggle: clear
